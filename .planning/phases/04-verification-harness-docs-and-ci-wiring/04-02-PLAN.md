---
phase: 04-verification-harness-docs-and-ci-wiring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
tags: [docs, mandate, changelog]

must_haves:
  truths:
    - "CLAUDE.md mandate section covers DOC-01..04 verbatim: all 8 TestPersistence_* names, the 7 mandated path patterns, the 2026-04-14 incident string, the no-flip-back RFC rule."
    - "CLAUDE.md mandate block references the script path scripts/verify-session-persistence.sh by name (supports SCRIPT-07)."
    - "CHANGELOG.md Unreleased section contains one line mentioning v1.5.2 and session persistence (DOC-05)."
    - "Only CLAUDE.md and CHANGELOG.md are modified; no mandated read-only code paths are touched."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Session persistence mandatory test coverage section — audited and patched where necessary"
      contains: "Session persistence: mandatory test coverage"
    - path: "CHANGELOG.md"
      provides: "One-line v1.5.2 session-persistence mention under [Unreleased] > ### Fixed"
      contains: "1.5.2"
  key_links:
    - from: "CLAUDE.md mandate section"
      to: "scripts/verify-session-persistence.sh"
      via: "path reference in mandate block"
      pattern: "verify-session-persistence\\.sh"
    - from: "CHANGELOG.md [Unreleased] > ### Fixed"
      to: "docs/SESSION-PERSISTENCE-SPEC.md"
      via: "relative link in the bullet"
      pattern: "SESSION-PERSISTENCE-SPEC"
---

<objective>
Audit the existing `CLAUDE.md` "Session persistence: mandatory test coverage" section (drafted at commit a262c6d) against DOC-01..DOC-04 verbatim requirements. If any grep-verifiable is missing, patch MINIMALLY — do not rewrite. Then add the one-line v1.5.2 hotfix mention to `CHANGELOG.md` under `## [Unreleased]` > `### Fixed` (DOC-05).

Purpose: Close the documentation side of the v1.5.2 milestone. The mandate section is the enforcement mechanism that PRs after this milestone must honor. CHANGELOG captures the fix for the eventual release notes without pre-creating a `[1.5.2]` heading (release tagging is deferred).

Output: Edits to `CLAUDE.md` (only if audit finds gaps) and `CHANGELOG.md` (always — adds one bullet). No other files touched.
</objective>

<execution_context>
@/home/ashesh-goplani/agent-deck/.worktrees/session-persistence/.claude/get-shit-done/workflows/execute-plan.md
@/home/ashesh-goplani/agent-deck/.worktrees/session-persistence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-verification-harness-docs-and-ci-wiring/04-CONTEXT.md
@CLAUDE.md
@CHANGELOG.md
@docs/SESSION-PERSISTENCE-SPEC.md
@internal/session/session_persistence_test.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit CLAUDE.md mandate section against DOC-01..04, patch gaps minimally</name>
  <files>CLAUDE.md</files>

  <read_first>
    - `.planning/phases/04-verification-harness-docs-and-ci-wiring/04-CONTEXT.md` — "CLAUDE.md mandate audit (locked — DOC-01..04)" section with the exact grep-verifiables.
    - `CLAUDE.md` (full file, lines 1-60) — current state of the "Session persistence: mandatory test coverage" section at line 5.
    - `.planning/REQUIREMENTS.md` lines 36-42 — exact wording of DOC-01..05 requirements.
    - `internal/session/session_persistence_test.go` lines 21-28 — canonical list of the eight TestPersistence_* names (for cross-check).
  </read_first>

  <action>
Run the audit greps in the repo root and record the counts. Use EXACTLY these checks (also used as acceptance criteria later):

```
# DOC-01: at least 8 TestPersistence_ mentions
DOC01=$(grep -c 'TestPersistence_' CLAUDE.md)

# DOC-02: all 6 mandated path prefixes appear; count distinct matches across the anchored tokens
DOC02=$(grep -cE 'internal/tmux|internal/session/instance|internal/session/userconfig|internal/session/storage|cmd/session_cmd|cmd/start_cmd|cmd/restart_cmd' CLAUDE.md)

# DOC-03: incident date string
DOC03=$(grep -c '2026-04-14' CLAUDE.md)

# DOC-04: default-flip-back rule
DOC04_FLAG=$(grep -c 'launch_in_user_scope' CLAUDE.md)
DOC04_RFC=$(grep -cE 'RFC' CLAUDE.md)

# SCRIPT-07 inline link (required for the CI wiring story to hold):
SCRIPT07=$(grep -c 'verify-session-persistence.sh' CLAUDE.md)

echo "DOC-01=${DOC01} (need >=8)"
echo "DOC-02=${DOC02} (need >=6)"
echo "DOC-03=${DOC03} (need >=1)"
echo "DOC-04 flag=${DOC04_FLAG} rfc=${DOC04_RFC} (need flag>=1, rfc>=1)"
echo "SCRIPT-07 inline=${SCRIPT07} (need >=1)"
```

**Baseline (recorded during planning): DOC-01=9, DOC-02>=6, DOC-03=2, DOC-04 flag=1 rfc=1, SCRIPT-07=3.** All thresholds already satisfied by the drafted mandate. This task's default outcome is "audit PASS, no patch".

Decision rule:
- If EVERY threshold is met: DO NOT modify `CLAUDE.md`. Skip directly to the commit step with `--allow-empty` NOT permitted — instead, commit nothing and report the audit in the SUMMARY.
- If any threshold is short:
  - Missing test names (DOC-01 < 8): append the exact missing names under the existing `### The eight required tests` numbered list.
  - Missing path (DOC-02 < 6): add the missing path as a bullet under `### Paths under the mandate`.
  - Missing 2026-04-14 (DOC-03 < 1): append one sentence to `### Why this exists` starting `The 2026-04-14 incident ...` — do not rewrite the block.
  - Missing launch_in_user_scope / RFC (DOC-04 shortfalls): append one bullet under `### Forbidden changes without an RFC` reading: `Flipping launch_in_user_scope default back to false on Linux.` (verbatim if already absent).
  - Missing SCRIPT-07 inline reference (SCRIPT-07 < 1): add one line to the first paragraph of the section: `bash scripts/verify-session-persistence.sh MUST run end-to-end on a Linux+systemd host and exit zero with every scenario reporting [PASS].`

Patches MUST be strictly additive. Do not rewrite existing sentences. Do not reorder headings. Do not touch any other section of `CLAUDE.md`.

If and only if `CLAUDE.md` was modified, run the greps again to confirm all thresholds are now met, then stage and commit:

```
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
docs(04-02): patch CLAUDE.md mandate gaps (DOC-0X)

Audited the "Session persistence: mandatory test coverage" section
against DOC-01..04 grep-verifiables. Patched <summarize the specific
gaps fixed> minimally; did not rewrite or reorder existing content.

Committed by Ashesh Goplani
EOF
)"
```

If `CLAUDE.md` was NOT modified (audit PASS as expected), there is no commit for this task — proceed to Task 2.
  </action>

  <acceptance_criteria>
    - `grep -c 'TestPersistence_' CLAUDE.md` >= 8 (DOC-01).
    - `grep -cE 'internal/tmux|internal/session/instance|internal/session/userconfig|internal/session/storage|cmd/session_cmd|cmd/start_cmd|cmd/restart_cmd' CLAUDE.md` >= 6 (DOC-02).
    - `grep -c '2026-04-14' CLAUDE.md` >= 1 (DOC-03).
    - `grep -c 'launch_in_user_scope' CLAUDE.md` >= 1 AND `grep -cE 'RFC' CLAUDE.md` >= 1 (DOC-04).
    - `grep -c 'verify-session-persistence.sh' CLAUDE.md` >= 1 (supports SCRIPT-07).
    - If `git diff HEAD~1 --name-only` includes `CLAUDE.md`, the diff is ADDITIVE only: `git log -1 --stat CLAUDE.md` shows zero deletions (or deletions only for accidental trailing whitespace), and the only sections touched are `### The eight required tests`, `### Paths under the mandate`, `### Why this exists`, `### Forbidden changes without an RFC`, or the opening paragraph of `## Session persistence: mandatory test coverage`.
    - No other file modified: `git log -1 --name-only` lists ONLY `CLAUDE.md` (or nothing if audit PASSed with no patch needed).
    - If a commit was made, `git log -1 --pretty=%B` contains `Committed by Ashesh Goplani` and does NOT contain `Co-Authored-By: Claude` or `Generated with Claude Code`.
  </acceptance_criteria>

  <verify>
    <automated>[ "$(grep -c 'TestPersistence_' CLAUDE.md)" -ge 8 ] && [ "$(grep -cE 'internal/tmux|internal/session/instance|internal/session/userconfig|internal/session/storage|cmd/session_cmd|cmd/start_cmd|cmd/restart_cmd' CLAUDE.md)" -ge 6 ] && [ "$(grep -c '2026-04-14' CLAUDE.md)" -ge 1 ] && [ "$(grep -c 'launch_in_user_scope' CLAUDE.md)" -ge 1 ] && [ "$(grep -cE 'RFC' CLAUDE.md)" -ge 1 ] && [ "$(grep -c 'verify-session-persistence.sh' CLAUDE.md)" -ge 1 ]</automated>
  </verify>

  <done>
    - Every DOC-01..04 threshold + SCRIPT-07 inline reference is met.
    - Either no commit (audit PASS) or exactly one additive commit touching only `CLAUDE.md`.
    - The SUMMARY records the baseline audit counts and the final counts.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add DOC-05 one-liner to CHANGELOG.md under [Unreleased]</name>
  <files>CHANGELOG.md</files>

  <read_first>
    - `.planning/phases/04-verification-harness-docs-and-ci-wiring/04-CONTEXT.md` — "DOC-05 — CHANGELOG mention (locked)" section with the exact bullet text.
    - `CHANGELOG.md` lines 1-30 — current state: `## [Unreleased]` is at line 8 with no body yet; `## [1.5.1]` starts at line 10.
    - `docs/SESSION-PERSISTENCE-SPEC.md` — the spec path referenced in the bullet.
  </read_first>

  <action>
Edit `CHANGELOG.md` to add one subsection (`### Fixed`) with one bullet under the existing `## [Unreleased]` heading. The `## [Unreleased]` heading is at line 8; line 9 is currently blank; line 10 is `## [1.5.1] - 2026-04-13`. We insert BETWEEN lines 9 and 10.

Insert EXACTLY these four lines between the existing line 9 and line 10 (i.e. after the blank line that follows `## [Unreleased]` and before `## [1.5.1]`):

```
### Fixed
- Session persistence: tmux servers now survive SSH logout on Linux+systemd hosts via `launch_in_user_scope` default (v1.5.2 hotfix). ([docs/SESSION-PERSISTENCE-SPEC.md](docs/SESSION-PERSISTENCE-SPEC.md))

```

(Note: the trailing blank line separates the new subsection from `## [1.5.1]`. Exactly ONE blank line between the bullet and `## [1.5.1]`.)

Do NOT create a `## [1.5.2]` heading. The hard rule is no push/tag/PR; release tagging is deferred. The bullet lives under `## [Unreleased]` and will migrate to a `[1.5.2]` block at release time.

After editing, confirm:

```
grep -n '## \[Unreleased\]' CHANGELOG.md         # still at line 8
grep -n '### Fixed' CHANGELOG.md | head -1       # new line ~10
grep -c '1.5.2' CHANGELOG.md                     # >= 1
grep -ciE 'session.persistence' CHANGELOG.md     # >= 1
```

Commit:

```
git add CHANGELOG.md
git commit -m "$(cat <<'EOF'
docs(04-02): add v1.5.2 session-persistence mention to CHANGELOG (DOC-05)

One-line bullet under [Unreleased] > ### Fixed per spec REQ-4 / DOC-05.
No [1.5.2] heading — release tagging is deferred (no push/tag/PR rule).

Committed by Ashesh Goplani
EOF
)"
```
  </action>

  <acceptance_criteria>
    - `grep -c '1.5.2' CHANGELOG.md` >= 1 (DOC-05).
    - `grep -ciE 'session.persistence' CHANGELOG.md` >= 1 (DOC-05).
    - `grep -c '## \[1.5.2\]' CHANGELOG.md` == 0 (no release heading — tagging deferred).
    - `grep -c 'SESSION-PERSISTENCE-SPEC' CHANGELOG.md` >= 1 (spec link present).
    - The `### Fixed` block sits UNDER `## [Unreleased]` and ABOVE `## [1.5.1]`: verify with `awk '/## \[Unreleased\]/{flag=1} /## \[1.5.1\]/{flag=0} flag' CHANGELOG.md | grep -c '### Fixed'` == 1.
    - No other file modified: `git log -1 --name-only` lists ONLY `CHANGELOG.md`.
    - `git log -1 --pretty=%B` contains `Committed by Ashesh Goplani` and does NOT contain `Co-Authored-By: Claude` or `Generated with Claude Code`.
    - No mandated-path file touched: `git log -1 --name-only | grep -E '^(internal/tmux|internal/session/instance.go|internal/session/userconfig.go|internal/session/storage|cmd/session_cmd|cmd/start_cmd|cmd/restart_cmd)'` returns nothing.
  </acceptance_criteria>

  <verify>
    <automated>[ "$(grep -c '1.5.2' CHANGELOG.md)" -ge 1 ] && [ "$(grep -ciE 'session.persistence' CHANGELOG.md)" -ge 1 ] && [ "$(grep -c '## \[1.5.2\]' CHANGELOG.md)" -eq 0 ] && [ "$(awk '/## \[Unreleased\]/{flag=1} /## \[1.5.1\]/{flag=0} flag' CHANGELOG.md | grep -c '### Fixed')" -eq 1 ]</automated>
  </verify>

  <done>
    - CHANGELOG.md has the one-line v1.5.2 bullet under `[Unreleased]` > `### Fixed`.
    - Single commit touching only CHANGELOG.md.
    - No `[1.5.2]` heading created.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs edits -> git | CLAUDE.md and CHANGELOG.md are text-only; no code paths touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | T (Tampering) | CLAUDE.md mandate section | mitigate | Audit rule requires ADDITIVE-only patches; `git log -1 --stat` deletion count is checked in acceptance criteria. |
| T-04-02-02 | R (Repudiation) | commit sign-off | mitigate | Commits end with `Committed by Ashesh Goplani`; acceptance criteria grep against `Co-Authored-By: Claude`. |
</threat_model>

<verification>
- Audit greps for DOC-01..04 all meet thresholds on final state.
- CHANGELOG grep for `1.5.2` and `session.persistence` (case-insensitive) each >= 1.
- No `[1.5.2]` heading present.
- No mandated-code-path file is modified in either commit.
</verification>

<success_criteria>
- CLAUDE.md audit passes (with or without additive patch).
- CHANGELOG.md has the one-liner under `[Unreleased]` > `### Fixed`.
- 0-2 commits landed, all signed `Committed by Ashesh Goplani`, no Claude attribution, no forbidden files modified.
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-harness-docs-and-ci-wiring/04-02-SUMMARY.md` with:
- Baseline grep counts for DOC-01..04 + SCRIPT-07 inline (before any edit).
- Final grep counts (after any patch).
- Whether CLAUDE.md was modified; if so, the diff summary.
- The CHANGELOG.md bullet exact text and placement line number.
- Commit SHA(s).
</output>
