---
phase: 04-verification-harness-docs-and-ci-wiring
plan: 04
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
  - 04-03
files_modified:
  - .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md
  - .planning/STATE.md
autonomous: true
requirements: []
tags: [verification, signoff, state]

must_haves:
  truths:
    - "bash scripts/verify-session-persistence.sh runs end-to-end on the local Linux+systemd conductor host and exits 0."
    - "Captured PASS output (with PIDs, cgroup paths, and claude argv) is committed to 04-VERIFY.md as the human-watchable proof."
    - ".planning/STATE.md Phase 4 status is flipped to complete with total_plans/completed_plans updated."
    - "No push, no tag, no PR. All commits signed Committed by Ashesh Goplani."
  artifacts:
    - path: ".planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md"
      provides: "End-to-end run output of the verification harness on the conductor host"
      contains: "OVERALL: PASS"
    - path: ".planning/STATE.md"
      provides: "Phase 4 signed off in the project state file"
      contains: "Phase 04"
  key_links:
    - from: "04-VERIFY.md"
      to: "scripts/verify-session-persistence.sh"
      via: "captured stdout + exit code of the end-to-end run"
      pattern: "OVERALL: PASS"
    - from: ".planning/STATE.md"
      to: "ROADMAP Phase 4 success criteria"
      via: "completed_phases counter and Phase 04 row"
      pattern: "completed_phases: 4"
---

<objective>
Execute `bash scripts/verify-session-persistence.sh` end-to-end on this Linux+systemd conductor host. Capture the full stdout / stderr into `.planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md` as the human-watchable proof that milestone criterion #4 is met (`docs/SESSION-PERSISTENCE-SPEC.md` success criterion). Then update `.planning/STATE.md` to sign Phase 4 off.

Purpose: Converts the shipped artifact from Plans 04-01/02/03 into an observed, recorded PASS. No new code, no new docs content beyond the verify report. This is the final sign-off plan of v1.5.2.

Output: `04-VERIFY.md` (new) and `.planning/STATE.md` (edited). Both committed.
</objective>

<execution_context>
@/home/ashesh-goplani/agent-deck/.worktrees/session-persistence/.claude/get-shit-done/workflows/execute-plan.md
@/home/ashesh-goplani/agent-deck/.worktrees/session-persistence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-verification-harness-docs-and-ci-wiring/04-CONTEXT.md
@CLAUDE.md
@scripts/verify-session-persistence.sh
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run the verification script end-to-end and capture output to 04-VERIFY.md</name>
  <files>.planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md</files>

  <read_first>
    - `scripts/verify-session-persistence.sh` — confirm it exists and is executable (should be true post 04-01).
    - `.planning/phases/04-verification-harness-docs-and-ci-wiring/04-CONTEXT.md` — "Scenarios the script MUST exercise" so you know what to expect in output.
    - `CLAUDE.md` — confirm no `rm`, confirm the mandate block references `verify-session-persistence.sh`.
    - `docs/SESSION-PERSISTENCE-SPEC.md` — milestone success criterion #4 (script must exit 0 with every scenario PASS/SKIP).
  </read_first>

  <action>
On this conductor host (Linux + systemd), run:

```
# 1. Build agent-deck from this worktree so the script exercises the code
#    actually on fix/session-persistence — not a stale globally-installed
#    binary. Put it at a deterministic path on PATH for the run.
mkdir -p ./bin
go build -o ./bin/agent-deck ./cmd/agent-deck
export PATH="$(pwd)/bin:${PATH}"

# 2. Confirm preflight is happy.
command -v agent-deck >/dev/null && echo "agent-deck ok ($(command -v agent-deck))" || { echo "ERROR: go build failed — agent-deck not on PATH"; exit 2; }
command -v tmux >/dev/null && echo "tmux ok"
command -v systemd-run >/dev/null && echo "systemd-run ok"

# 3. Run the script and tee output to a tempfile. Capture exit code.
TMPLOG=$(mktemp -t adeck-verify-run.XXXXXX.log)
set +e
bash scripts/verify-session-persistence.sh 2>&1 | tee "${TMPLOG}"
RC=${PIPESTATUS[0]}
set -e
echo "exit=${RC}"
```

If `RC` != 0: STOP. Investigate. The fix path is:
- Most likely the harness needs a minor adjustment (seeding ClaudeSessionID, session-start timing). Fix in `scripts/verify-session-persistence.sh` under the umbrella of Plan 04-01 (open an amendment commit on that plan, not here).
- Do NOT commit a failing 04-VERIFY.md.
- Do NOT mark Phase 4 complete in STATE.md.

If `RC` == 0: write `.planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md` with this shape:

```markdown
# Phase 04 — verification run

**Date:** <YYYY-MM-DD> (conductor host, `uname -a` below)
**Script:** scripts/verify-session-persistence.sh @ <git rev-parse --short HEAD>
**Exit code:** 0

## Host

```
<paste `uname -a` output>
<paste `systemctl --user is-system-running` output>
<paste `command -v agent-deck`, `command -v tmux`, `command -v systemd-run`>
```

## Script output

```
<paste the FULL contents of ${TMPLOG} here — do not truncate. PIDs,
cgroup paths, claude argv, and every [PASS]/[SKIP]/[FAIL] line must
be preserved verbatim so a reviewer can read the exact evidence.>
```

## Verdict

- Scenario 1 (live session + cgroup inspection): <PASS|SKIP>
- Scenario 2 (login-session teardown survival): <PASS|SKIP>
- Scenario 3 (stop -> restart resume argv): <PASS|SKIP>
- Scenario 4 (fresh session argv shape): <PASS|SKIP>

Overall: **PASS** — milestone criterion #4 from docs/SESSION-PERSISTENCE-SPEC.md is satisfied on this host.
```

Commit (note: `.planning/` is git-excluded via `.git/info/exclude`, so `-f` is required):

```
git add -f .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md
git commit -m "$(cat <<'EOF'
docs(04-04): capture end-to-end verify-session-persistence.sh run

Ran bash scripts/verify-session-persistence.sh on the conductor host,
exit 0, every scenario PASS or SKIP. Full output (PIDs, cgroup paths,
claude argv) recorded in 04-VERIFY.md as the human-watchable proof
for milestone criterion #4.

Committed by Ashesh Goplani
EOF
)"
```
  </action>

  <acceptance_criteria>
    - `test -f .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md` returns 0.
    - `grep -c 'Exit code: 0' .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md` >= 1.
    - `grep -c 'OVERALL: PASS\|Overall: \*\*PASS\*\*' .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md` >= 1.
    - `grep -Ec '\[PASS\]|\[SKIP\]' .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md` >= 4 (one line per scenario that ran).
    - `grep -c '\[FAIL\]' .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md` == 0.
    - `grep -c 'PID=' .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md` >= 1 (at least one captured PID line in the pasted output).
    - `git log -1 --name-only` lists ONLY `.planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md`.
    - `git log -1 --pretty=%B` contains `Committed by Ashesh Goplani` and does NOT contain `Co-Authored-By: Claude` or `Generated with Claude Code`.
    - `test -x ./bin/agent-deck` returns 0 (build step produced a runnable binary from this worktree before the script ran).
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md && grep -q 'Exit code: 0' .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md && ! grep -q '\[FAIL\]' .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md && [ "$(grep -Ec '\[PASS\]|\[SKIP\]' .planning/phases/04-verification-harness-docs-and-ci-wiring/04-VERIFY.md)" -ge 4 ]</automated>
  </verify>

  <done>
    - Harness ran, exited 0, output captured verbatim in 04-VERIFY.md.
    - Single commit lands the verify report with the required sign-off.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update .planning/STATE.md to sign Phase 4 off</name>
  <files>.planning/STATE.md</files>

  <read_first>
    - `.planning/STATE.md` — current state: line 5 `status: executing`, line 7 `stopped_at: "Phase 03 fully landed. Next step: /gsd-plan-phase 4"`, line 8 `last_updated: "2026-04-14T13:26:20Z"`, line 11 `completed_phases: 3`, line 12 `total_plans: 13`, line 13 `completed_plans: 13`, line 24 `**Current focus:** Phase 04 — verification-harness-docs-and-ci-wiring (not started; Phase 03 complete 2026-04-14)`, line 28 `Phase: 04 (verification-harness-docs-and-ci-wiring) — NOT STARTED`, line 50 `| 4. Verification harness, docs, and CI wiring | 0/TBD | 0m | — |`.
    - `.planning/ROADMAP.md` Phase 4 section — 4 plans total.
    - `CLAUDE.md` — confirm no push/tag/PR; this task edits STATE.md only.
  </read_first>

  <action>
Edit `.planning/STATE.md` with EXACTLY these changes (use the Read tool to see the current state first so line numbers are confirmed; the values below are the targets):

1. **Frontmatter** (lines 1-15):
   - `status: executing` → `status: ready-for-signoff`
   - `stopped_at: "Phase 03 fully landed. Next step: /gsd-plan-phase 4"` → `stopped_at: "Phase 04 complete — v1.5.2 milestone ready for user sign-off (no push/tag/PR per hard rule)"`
   - `last_updated: "2026-04-14T13:26:20Z"` → `last_updated: "<current ISO-8601 UTC, e.g. 2026-04-14T21:15:00Z>"`
   - `last_activity: 2026-04-14 -- Phase 03 complete — REQ-2 GREEN, OBS-02 landed, lifecycle doc updated` → `last_activity: 2026-04-14 -- Phase 04 complete — verification harness shipped, CLAUDE.md mandate audited, CI workflow wired, end-to-end run PASS`
   - `completed_phases: 3` → `completed_phases: 4`
   - `total_plans: 13` → `total_plans: 17`
   - `completed_plans: 13` → `completed_plans: 17`

2. **Body paragraph around line 24**: replace `**Current focus:** Phase 04 — verification-harness-docs-and-ci-wiring (not started; Phase 03 complete 2026-04-14)` with `**Current focus:** v1.5.2 milestone complete on branch fix/session-persistence — awaiting user sign-off (no push/tag/PR allowed).`

3. **Current Position** block (around lines 28-33): update
   - `Phase: 04 (verification-harness-docs-and-ci-wiring) — NOT STARTED` → `Phase: 04 (verification-harness-docs-and-ci-wiring) — COMPLETE`
   - `Plan: 0 of TBD` → `Plan: 4 of 4`
   - `Status: Phase 03 complete — ready to plan Phase 04` → `Status: Phase 04 complete — v1.5.2 shippable pending user sign-off`
   - `Last activity: 2026-04-14 -- Phase 03 complete — REQ-2 GREEN, OBS-02 landed, lifecycle doc updated` → `Last activity: 2026-04-14 -- Phase 04 complete — scripts/verify-session-persistence.sh PASS, CI workflow landed`
   - `Progress: [██████████] 100%` — leave at 100% (matches completed / total ratio).

4. **Performance Metrics table** (around line 50): replace `| 4. Verification harness, docs, and CI wiring | 0/TBD | 0m | — |` with `| 4. Verification harness, docs, and CI wiring | 4/4 | — | — |`.

5. **Session Continuity** (bottom of file, around line 83): update
   - `Last session: 2026-04-14 — Phase 03 execution complete` → `Last session: 2026-04-14 — Phase 04 execution complete`
   - `Stopped at: Phase 03 fully landed. Next step: /gsd-plan-phase 4` → `Stopped at: Phase 04 fully landed. v1.5.2 milestone complete. Next step: user sign-off + manual SSH-logout verification per milestone criterion #2.`

Do NOT modify any other line. Do NOT edit `docs/`, `internal/`, `cmd/`, or any mandated-path file.

Commit:

```
git add -f .planning/STATE.md
git commit -m "$(cat <<'EOF'
docs(04-04): sign off Phase 04 in .planning/STATE.md

Phase 4 complete: scripts/verify-session-persistence.sh shipped +
executed on conductor host (exit 0), CLAUDE.md mandate audit PASS,
CHANGELOG.md DOC-05 line added, .github/workflows/session-persistence.yml
wired. v1.5.2 milestone ready for user sign-off. Per hard rule: no
push, no tag, no PR.

Committed by Ashesh Goplani
EOF
)"
```
  </action>

  <acceptance_criteria>
    - `grep -c 'completed_phases: 4' .planning/STATE.md` == 1.
    - `grep -c 'total_plans: 17' .planning/STATE.md` == 1.
    - `grep -c 'completed_plans: 17' .planning/STATE.md` == 1.
    - `grep -c 'Phase 04' .planning/STATE.md` >= 1.
    - `grep -c 'COMPLETE' .planning/STATE.md` >= 1.
    - `grep -c '4/4' .planning/STATE.md` >= 1.
    - `git log -1 --name-only` lists ONLY `.planning/STATE.md`.
    - `git log -1 --pretty=%B` contains `Committed by Ashesh Goplani` and does NOT contain `Co-Authored-By: Claude` or `Generated with Claude Code`.
    - No mandated-code-path file touched: `git log -1 --name-only | grep -E '^(internal/tmux|internal/session/instance.go|internal/session/userconfig.go|internal/session/storage|cmd/session_cmd|cmd/start_cmd|cmd/restart_cmd)'` returns nothing.
    - No push/tag/PR: `git log origin/fix/session-persistence..HEAD` shows commits not yet on remote (branch has NOT been pushed).
  </acceptance_criteria>

  <verify>
    <automated>[ "$(grep -c 'completed_phases: 4' .planning/STATE.md)" -eq 1 ] && [ "$(grep -c 'total_plans: 17' .planning/STATE.md)" -eq 1 ] && [ "$(grep -c 'completed_plans: 17' .planning/STATE.md)" -eq 1 ] && grep -q 'Phase 04' .planning/STATE.md && grep -q 'COMPLETE' .planning/STATE.md</automated>
  </verify>

  <done>
    - STATE.md reflects Phase 4 complete and v1.5.2 ready for sign-off.
    - Single signed commit.
    - No other file modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness -> systemd user bus | Same as Plan 04-01's boundary. Runs as invoking user. |
| planning docs edits | `.planning/` is git-excluded via `.git/info/exclude`; `git add -f` is required. No production code touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | T (Tampering) | STATE.md edits | mitigate | Edit list is line-specific and additive-only in the body; acceptance criteria grep each expected key/value. |
| T-04-04-02 | R (Repudiation) | commit sign-off | mitigate | Sign-off `Committed by Ashesh Goplani`; grep-checked in acceptance criteria. |
| T-04-04-03 | E (Elevation) | harness execution under user scope | accept | Harness runs under the invoking user, uses systemd user bus. No root required. Destructive loginctl terminate is off by default. |
</threat_model>

<verification>
- Harness exited 0 (captured in 04-VERIFY.md).
- STATE.md frontmatter math: completed_phases (4) × total_plans (17) math is consistent with roadmap (Phase 1=2, Phase 2=6, Phase 3=5, Phase 4=4 → 17 total).
- No push, no tag, no PR. Branch remains local.
</verification>

<success_criteria>
- Milestone criterion #4 from `docs/SESSION-PERSISTENCE-SPEC.md` ("`bash scripts/verify-session-persistence.sh` runs end-to-end on the conductor host and exits 0") is observably satisfied and recorded.
- STATE.md shows Phase 4 complete and the milestone ready for sign-off.
- Two commits land (one per task), both signed `Committed by Ashesh Goplani`, neither modifies mandated-code-path files.
</success_criteria>

<output>
After completion, create `.planning/phases/04-verification-harness-docs-and-ci-wiring/04-04-SUMMARY.md` with:
- The two commit SHAs.
- Link/reference to 04-VERIFY.md.
- Final state values from STATE.md (completed_phases, total_plans, completed_plans).
- Explicit reminder to the user: v1.5.2 is shippable; next user-driven steps (outside this phase) are: manual SSH-logout test per milestone criterion #2, and a separate release cycle for tagging / pushing / PR. These are explicitly OUT OF SCOPE of Phase 4.
</output>
