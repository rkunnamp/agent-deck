---
phase: 03-resume-on-start-and-error-recovery-req-2-fix
plan: 05
type: execute
wave: 5
depends_on:
  - 03-04
files_modified:
  - docs/session-id-lifecycle.md
  - .planning/STATE.md
autonomous: false
requirements:
  - PERSIST-10
tags: [docs, state, phase-signoff]

must_haves:
  truths:
    - "docs/session-id-lifecycle.md contains a new subsection titled '## Start / Restart Dispatch' listing the four CONTEXT Decision 6 invariants verbatim."
    - "The pre-existing 'Invariants' section is unchanged; the new subsection is ADDITIVE."
    - ".planning/STATE.md is updated: Phase 3 marked COMPLETE in the progress table; decisions section references Plan 03-01..05 outcomes."
    - "One final full `TestPersistence_*` suite run is captured showing 13 tests GREEN (or SKIP with the mandated prefix)."
  artifacts:
    - path: "docs/session-id-lifecycle.md"
      provides: "Start / Restart Dispatch subsection"
      contains: "## Start / Restart Dispatch"
    - path: ".planning/STATE.md"
      provides: "Phase 3 completion record"
      contains: "Phase 03"
  key_links:
    - from: "docs/session-id-lifecycle.md §Start / Restart Dispatch"
      to: "PERSIST-10 (ROADMAP + REQUIREMENTS)"
      via: "Invariants 1-4 per CONTEXT Decision 6"
      pattern: "Instance.ClaudeSessionID in instance JSON storage is the sole authoritative"
---

<objective>
Close Phase 3 by landing the additive `## Start / Restart Dispatch` subsection in `docs/session-id-lifecycle.md` per CONTEXT Decision 6 and updating `.planning/STATE.md` to mark Phase 3 complete.

Purpose: PERSIST-10 requires the lifecycle doc to remain honored. This subsection makes the Phase 3 routing rule and its invariants explicit and future-proof — any refactor that violates the rule has a clearly-authored contract to check against.

Output:
- `docs/session-id-lifecycle.md` — new subsection appended after existing invariants.
- `.planning/STATE.md` — Phase 3 row updated; decisions + blockers + last_activity updated.
- One final verification run of the full persistence suite captured in the Plan SUMMARY.

Autonomous: false — final phase sign-off requires human verification that all tests still green before committing the STATE update.
</objective>

<execution_context>
@/home/ashesh-goplani/agent-deck/.worktrees/session-persistence/.claude/get-shit-done/workflows/execute-plan.md
@/home/ashesh-goplani/agent-deck/.worktrees/session-persistence/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-resume-on-start-and-error-recovery-req-2-fix/03-CONTEXT.md
@docs/session-id-lifecycle.md
@docs/SESSION-PERSISTENCE-SPEC.md
@internal/session/instance.go
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append "Start / Restart Dispatch" subsection to docs/session-id-lifecycle.md</name>
  <files>docs/session-id-lifecycle.md</files>

  <read_first>
    - `docs/session-id-lifecycle.md` (entire file — the 46 existing lines containing `## Invariants`, `## Creation and Persistence`, `## Reconnect / Restart`, `## Fork / Clear / ID Changes`, `## Event Log Schema`).
    - `.planning/phases/03-resume-on-start-and-error-recovery-req-2-fix/03-CONTEXT.md` Decision 6 (lines 128-136) — exact invariants 1-4 to copy verbatim.
    - `internal/session/instance.go` lines 4111-4178 to confirm helper signature + behavior referenced by invariants 2-4.
  </read_first>

  <action>
Append the following NEW subsection to `docs/session-id-lifecycle.md` at the END of the file, AFTER the `## Event Log Schema` section. Do NOT modify or reorder any existing content. The four invariants are copied VERBATIM from CONTEXT Decision 6.

```markdown

## Start / Restart Dispatch

This section was added in v1.5.2 (Phase 3 of the session-persistence hotfix).
It pins the routing rule for Claude session starts introduced by the
2026-04-14 incident fix. Any refactor that violates these invariants
regresses REQ-2 and must include a matching amendment here.

1. `Instance.ClaudeSessionID` in instance JSON storage is the sole authoritative source for the Claude session UUID bound to an agent-deck instance.
2. `Start()`, `StartWithMessage()`, and `Restart()` route through `buildClaudeResumeCommand()` whenever `ClaudeSessionID != ""`. They never mint a new UUID for an instance that already has one.
3. `buildClaudeResumeCommand()` uses `claude --resume <id>` when JSONL evidence exists and `claude --session-id <id>` otherwise. In both cases `<id>` is the stored `ClaudeSessionID` — never a newly minted UUID.
4. Disk scans of `~/.claude/projects/<hash>/*.jsonl` remain non-authoritative. `sessionHasConversationData()` is a presence check, NOT an identity probe.

### Enforcement

The following tests in `internal/session/session_persistence_test.go` pin these invariants and are part of the CLAUDE.md mandate gate:

- `TestPersistence_RestartResumesConversation` — invariant 2 + 3 (Restart branch).
- `TestPersistence_StartAfterSIGKILLResumesConversation` — invariant 2 + 3 (Start branch).
- `TestPersistence_ClaudeSessionIDSurvivesHookSidecarDeletion` — invariant 1.
- `TestPersistence_FreshSessionUsesSessionIDNotResume` — invariant 3 (no-JSONL fallback).
- `TestPersistence_ClaudeSessionIDPreservedThroughStopError` — invariant 1 + 2 (no implicit clear on status transitions).
- `TestPersistence_SessionIDFallbackWhenJSONLMissing` — invariant 3 + 4 (no disk-scan rebind when stored ID's JSONL is absent).
- `TestPersistence_ResumeLogEmitted_*` (three variants) — OBS-02 audit line records every dispatch decision.

A PR that removes any of the eight CLAUDE.md-mandated tests above, or that adds a code path which assigns `i.ClaudeSessionID = ""` outside `Fork()` (target-side) or `Delete()`, violates this section and requires an RFC.
```

Keep the trailing newline. Do not touch any line above this section.

Commit:
```
docs(03-05): add Start / Restart Dispatch subsection to session-id-lifecycle

PERSIST-10: adds the four invariants locking in the REQ-2 routing rule
landed by Plans 03-01..03-04. Additive — existing sections unchanged.

Committed by Ashesh Goplani
```
  </action>

  <acceptance_criteria>
    - `grep -n '## Start / Restart Dispatch' docs/session-id-lifecycle.md` returns exactly one match.
    - `grep -n 'Instance.ClaudeSessionID in instance JSON storage is the sole authoritative' docs/session-id-lifecycle.md` returns exactly one match (invariant 1 verbatim).
    - `grep -n 'never mint a new UUID for an instance that already has one' docs/session-id-lifecycle.md` returns one match (invariant 2 verbatim).
    - `grep -n 'claude --resume' docs/session-id-lifecycle.md` — invariant 3 landed.
    - `grep -n 'Disk scans of' docs/session-id-lifecycle.md` — invariant 4 landed.
    - `grep -c '^## ' docs/session-id-lifecycle.md` returns exactly 6 (5 existing + 1 new).
    - The pre-existing `## Invariants` section still contains its original 4 invariants (line-for-line unchanged) — verified by `diff HEAD~1 HEAD -- docs/session-id-lifecycle.md` showing ONLY additions (no `-` removal lines besides the trailing newline shift).
    - `git diff HEAD~1 HEAD --stat -- docs/session-id-lifecycle.md` shows only insertions.
    - Commit body has no Claude attribution.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^## ' docs/session-id-lifecycle.md && grep -n 'Start / Restart Dispatch' docs/session-id-lifecycle.md && grep -n 'sole authoritative source' docs/session-id-lifecycle.md</automated>
  </verify>

  <done>
    - New subsection exists with the four invariants verbatim + enforcement paragraph.
    - Existing sections untouched.
    - Single commit landed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full persistence suite one last time; update .planning/STATE.md with Phase 3 completion</name>
  <files>.planning/STATE.md</files>

  <read_first>
    - `.planning/STATE.md` (entire file — current frontmatter + Current Position + Accumulated Context sections).
    - `.planning/ROADMAP.md` Phase 3 row in the progress table.
    - The four summary files created by Plans 03-01 through 03-04 (if present in the phase dir).
  </read_first>

  <action>
Step 1: Run the final verification.

```
go test -run TestPersistence_ ./internal/session/... -race -count=1 -v > /tmp/phase3-final-suite.log 2>&1
echo "EXIT=$?"
```

Expected: EXIT=0 with 13 PASS lines (or SKIP lines carrying the mandated prefix on a host without systemd/tmux). Copy the summary lines into the SUMMARY.md for this plan.

Step 2: Update `.planning/STATE.md` — modify the following fields/sections IN PLACE. Do not rewrite the whole file.

Frontmatter:
- `status:` → `executing` → keep as-is UNTIL Phase 4 planning begins; Phase 3 complete is represented by the phase table row below, not the global milestone status.
- `last_updated:` → set to the ISO-8601 UTC timestamp of this task's execution.
- `last_activity:` → `2026-04-14 -- Phase 03 complete — REQ-2 GREEN, OBS-02 landed, lifecycle doc updated`.
- `progress.completed_phases:` → 2 (Phase 01 was already complete; Phase 03 now adds one — BUT double-check by counting Phase 02's current status in the table; if Phase 02 also counts as completed at this point, set to 3 accordingly).
- `progress.total_plans:` → bump by 5 (Plan 03-01..03-05).
- `progress.completed_plans:` → bump by 5.
- `progress.percent:` → recompute as `round(completed_plans / total_plans * 100)`.

Current Position section:
- `Phase:` → `04 (verification-harness-docs-and-ci-wiring) — NOT STARTED`.
- `Plan:` → `0 of TBD`.
- `Status:` → `Phase 03 complete — ready to plan Phase 04`.
- `Last activity:` → mirror the frontmatter line.

Performance Metrics table — update the Phase 3 row:
- Change `| 3. Resume-on-start and error-recovery (REQ-2 fix) | 0/TBD | 0m | — |` to `| 3. Resume-on-start and error-recovery (REQ-2 fix) | 5/5 | <actual total> | <avg> |`.
- Duration values come from the individual Plan SUMMARY files' recorded timings; if not yet written, use `—` for avg and leave the rest accurate.

Accumulated Context / Decisions:
- Append bullet: `Phase 03: routed Start() and StartWithMessage() through buildClaudeResumeCommand when ClaudeSessionID != "" — closed the 2026-04-14 f1e103df/b9403638 divergence. OBS-02 per-call audit line landed. docs/session-id-lifecycle.md gained a Start / Restart Dispatch subsection (PERSIST-10).`

Session Continuity:
- `Last session:` → `2026-04-14 — Phase 03 execution complete`.
- `Stopped at:` → `Phase 03 fully landed. Next step: /gsd-plan-phase 4`.
- `Resume file:` → `None`.

Step 3: Commit.

```
docs(03-05): sign off Phase 03 in .planning/STATE.md

Phase 03 (resume-on-start-and-error-recovery-req-2-fix) complete:
  - REQ-2 closed: Start/StartWithMessage route through
    buildClaudeResumeCommand when ClaudeSessionID != "".
  - PERSIST-07..10 satisfied.
  - OBS-02 / OBS-03 satisfied (per-call "resume: " Info line).
  - 13 TestPersistence_* functions all GREEN on Linux+systemd+tmux host.
  - docs/session-id-lifecycle.md PERSIST-10 subsection landed.

Next: /gsd-plan-phase 4 (verification harness, docs, CI wiring).

Committed by Ashesh Goplani
```

Use `git add -f .planning/STATE.md` (the `.planning/` tree is in `.git/info/exclude` — per CONTEXT decision 10).
  </action>

  <acceptance_criteria>
    - `go test -run TestPersistence_ ./internal/session/... -race -count=1` exits 0 (FINAL suite check).
    - `grep -c 'TestPersistence_' /tmp/phase3-final-suite.log` shows at least 13 test-function executions (8 mandated + 2 Plan-01/02 + 3 Plan-04 log tests).
    - `grep -c '^--- FAIL' /tmp/phase3-final-suite.log` returns 0.
    - `.planning/STATE.md` `last_activity` line mentions `Phase 03 complete`.
    - `.planning/STATE.md` frontmatter `progress.completed_plans` increased by 5 from the pre-edit value.
    - `grep 'Phase 03' .planning/STATE.md` returns at least one match after the edit.
    - Commit landed via `git add -f .planning/STATE.md && git commit -m "..."` (forced-add due to `.git/info/exclude`).
    - Commit body contains no Claude attribution.
  </acceptance_criteria>

  <verify>
    <automated>go test -run TestPersistence_ ./internal/session/... -race -count=1 -v 2>&1 | grep -E '^(--- PASS|--- FAIL|--- SKIP|PASS|FAIL|ok )' | tee /tmp/phase3-final-suite.log ; echo '---STATE check---' ; grep -E 'last_activity|completed_plans|Phase 03' .planning/STATE.md | head -10</automated>
  </verify>

  <done>
    - Full persistence suite GREEN.
    - STATE.md updated with Phase 3 completion and next-step pointer.
    - Commit landed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase 03 sign-off — human verifies end-to-end</name>

  <what-built>
    Phase 03 complete. REQ-2 closed. OBS-02 live. PERSIST-07..10 pinned by 6 tests. docs/session-id-lifecycle.md updated. .planning/STATE.md rolled forward.
  </what-built>

  <how-to-verify>
    1. Read the final verification log at `/tmp/phase3-final-suite.log` — confirm 13 PASS (or clean SKIP) and zero FAIL across the TestPersistence_* suite.
    2. Inspect git log for the 5 Phase-3 commits:
       `git log --oneline --reverse 7d76ee1..HEAD`
       Expect five commit subjects starting with `test(03-01)`, `test(03-02)`, `feat(03-03)`, `feat(03-04)` or `test(03-04)` + `feat(03-04)`, `docs(03-05)`, `docs(03-05)`.
    3. Grep final repo state:
       - `grep -c 'TestPersistence_' internal/session/session_persistence_test.go` should show the suite grew by 3 (Plan 03-01 + 03-02 + 3 log-emit tests) from the Phase 1 baseline of 8 mandated tests + 1 ExplicitOptOut pin.
       - `grep -c 'resume: ' internal/session/instance.go` should show at least 4 emission sites.
       - `grep -c '## Start / Restart Dispatch' docs/session-id-lifecycle.md` should return 1.
    4. Confirm no pushes/tags were made: `git status -sb` and `git log origin/fix/session-persistence..HEAD --oneline` (if the remote branch exists).
    5. Confirm nothing outside the authorized paths was touched: `git diff --stat 7d76ee1..HEAD -- ':!internal/session/instance.go' ':!internal/session/session_persistence_test.go' ':!docs/session-id-lifecycle.md' ':!.planning/'` should show no entries.
  </how-to-verify>

  <resume-signal>Type "approved — Phase 03 SIGNED OFF" or report any failing check.</resume-signal>


  <files>(no files modified — human verification only)</files>

  <action>
This is a human-verify checkpoint. Execute the commands listed in <how-to-verify> above and present the output to the user. Do NOT proceed until the user replies with the approval string from <resume-signal>.
  </action>

  <verify>
    <automated>echo 'Checkpoint requires human approval per <resume-signal>; no automated gate.'</automated>
  </verify>

  <done>
    - User replied with the exact approval string from <resume-signal>.
    - No FAIL lines surfaced during the <how-to-verify> commands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ markdown -> repo | Documentation-only change, no runtime impact. |
| .planning/STATE.md -> git | Forced-add (in .git/info/exclude); local-only tracking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | T | Docs-only drift | accept | Documentation — no runtime behavior. Enforced by tests in prior plans. |
</threat_model>

<verification>
- Phase 3 success criteria (ROADMAP.md lines 78-86) all observable as GREEN:
    - TEST-05/06/07/08 GREEN (from Plan 03-03).
    - PERSIST-08 regression guard GREEN (Plan 03-01).
    - `resume: ` grep substrings present (Plan 03-04).
    - session-id-lifecycle.md invariants honored + new subsection (Plan 03-05).
- STATE.md reflects completion.
</verification>

<success_criteria>
- All 6 Phase-3 requirements closed (PERSIST-07..10, OBS-02, OBS-03).
- Phase 03 sign-off checkpoint approved.
- User knows next step: `/gsd-plan-phase 4`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-resume-on-start-and-error-recovery-req-2-fix/03-05-SUMMARY.md` containing:
- Final full-suite output from /tmp/phase3-final-suite.log.
- Git log of the 5 Phase-3 commits with their SHAs.
- Summary table: requirement ID -> plan that closed it -> test that pins it.
</output>
